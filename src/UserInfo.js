function UserInfo({ balance, loan }) {
  return (
    <div>
      <h1>useReducer Bank Account</h1>
      <p>Balance: {balance}</p>
      <p>Loan: {loan}</p>
    </div>
  );
}

export default UserInfo;
